{
  "name": "todo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "domtor": "../.."
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  },
  "scripts": {
    "build": "esbuild script.ts --bundle --minify --sourcemap --outfile=dist/index.js",
    "watch": "esbuild script.ts --bundle --sourcemap --outfile=dist/index.js --watch"
  }
}